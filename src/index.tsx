import React from "react";
import ReactDOM from "react-dom";

const App = () => (
  <>
    <h1>Hello React, Esbuild, Webpack, Esbuild-Loader</h1>
    <p>Enjoy the blazing fast build</p>
  </>
);

ReactDOM.render(<App />, document.getElementById("root"));
